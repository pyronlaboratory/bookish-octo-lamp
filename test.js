/**
 * @description Logs "0" to the console when invoked. It is a constant function,
 * meaning it always returns the same output and does not take any input parameters.
 * The function is defined using an arrow function syntax with a single expression statement.
 */
const fn = () => {


  console.log(0);

};
